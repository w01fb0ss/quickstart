module.exports = {
    singleQuote: false,
    semi: false, // make sure to know when they're required! tabWidth: 4,
    endOfLine: 'lf', // fewer bytes, same result
    trailingComma: 'es5', // hold over from golang
    printWidth: 80, // controversial, but avoids wrapping when editing side-by-side
    bracketSpacing: true, // current style seems to follow this, so might as well make standard
}
